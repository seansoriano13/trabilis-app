feat: enhance booking system with improved error handling, validation, and admin notifications

Major improvements across booking workflows, email services, and admin dashboard:

Backend Changes:
- Email Service (brevoEmailService.js):
  * Add comprehensive validation and error handling for tour booking PDF generation
  * Implement normalization functions for tour booking records (normalizeTourBookingRecord, normalizeInclusionGroup)
  * Add JSON parsing utilities (parseJsonField, ensureArray) for handling database JSON fields
  * Enhance error messages with detailed logging and stack traces
  * Add validation for transformed booking structure before PDF generation
  * Improve HTML generation error handling with try-catch blocks

- Webhook Controller (webhookController.js):
  * Add metadata validation for Stripe webhook events
  * Implement automatic slot decrementing for tour bookings when payment is confirmed
  * Add atomic slot management to prevent race conditions
  * Enhance error handling for email sending with detailed error logging
  * Integrate admin notifications system for booking events
  * Add validation for booking_reference in webhook metadata
  * Improve logging with session ID and payment details

- Admin Controllers:
  * Dashboard: Add configurable pagination (max 100 items per page, default 20)
  * Dashboard: Change ordering to show most recent bookings first
  * Dashboard: Improve error handling with detailed error logging
  * Dashboard: Add pagination metadata in API responses
  * Add admin notification integration across all admin controllers
  * Improve error handling in cancellation, appointment, flight, and tour controllers

- Booking Controllers:
  * Enhance error handling in flightBookingController and tourBookingController
  * Add admin notifications for booking events
  * Improve validation and error messages

- Services:
  * assignmentService: Enhanced error handling and logging
  * cancellationService: Improved error handling
  * createAmadeusOrderService: Add admin notifications integration

- Middleware:
  * errorHandler: Enhanced error logging and response formatting

- Utils:
  * tourBookingHtmlGenerator: Minor cleanup and improvements

Frontend Changes:
- TourBookingSuccess.jsx:
  * Implement improved polling logic with error handling
  * Add consecutive error tracking (max 3 errors before stopping)
  * Add proper cleanup for polling intervals
  * Improve error messages and user feedback

- FlightBookingSuccess.jsx:
  * Minor improvements and bug fixes

- TourBooking.jsx:
  * Enhance booking flow and error handling

- AdminNavbar.jsx:
  * Update navigation and UI improvements

Database:
- Add new database directory structure (untracked)

Summary:
- 21 files changed: 1,258 insertions(+), 426 deletions(-)
- Focus on error handling, validation, and admin notification system integration
- Improved booking workflow reliability and user experience
- Enhanced debugging capabilities with comprehensive logging

